<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Package-visibility declarations (Android 11+): needed so PackageManager can tell us
         whether these player apps are installed, for the Settings quick-launch shortcuts. -->
    <queries>
        <package android:name="com.spotify.music" />
        <package android:name="com.google.android.apps.youtube.music" />
        <package android:name="com.aspiro.tidal" />
    </queries>

    <application
        android:name=".VisualizerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.NeverSpiral"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NeverSpiral"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".AudioCaptureService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

        <!-- android:exported="true" is required here: the launcher (a separate app/process)
             sends the APPWIDGET_UPDATE broadcast, and the system only delivers it to receivers
             that are actually reachable, i.e. exported. -->
        <receiver
            android:name=".VisualizerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- android:exported="true" is required here too: Quick Settings binds to tile services
             from the system process, protected by BIND_QUICK_SETTINGS_TILE (a signature|privileged
             permission only the system holds), the same pattern every QS tile app uses. -->
        <service
            android:name=".VisualizerTileService"
            android:label="@string/app_name"
            android:icon="@drawable/ic_tile_bars"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".NowPlayingListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Grants the system package installer read access to the OTA APK downloaded into our
             private external-files storage; DownloadManager's own getUriForDownloadedFile() is
             built for the public Downloads collection and doesn't reliably work for that. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
